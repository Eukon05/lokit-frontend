export type DeviceResponse = {
    id: string,
    name: string,
    description: string,
    physicalAddress: string,
    hasActiveToken: boolean,
    roomId: string,
    createdAt: string,
    updatedAt: string
}